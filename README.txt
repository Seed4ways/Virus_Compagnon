projet sécurité MOLL Manutea - DESHAIES Alix

a faire (global):
3 autres petits exec de con qui demande un truc a l'utilisateur
une interface réutilisable pour tous (de preference sdl2)
le virus type compagnon
rapport technique 15 pages pour la présentation
tous ce qu'il y a sur la fiche du projet (papier)

img - 6 images avec 3 extensions différentes (jpg, gif et png)
cmake et make oublie pas je trouve c plus propre et ça fera plaisir au profs (ta la struct dans pg1 si ta oublié)

#########################################################################################################################################################

MonPG1 :
Le lecteur d'image

utilise la librairie stb (pour gérer les images c pas trop mal et simple a utiliser)
requier stb_image.h : https://github.com/nothings/stb/blob/master/stb_image.h (déja dans le fichier include du projet)

et sdl2 pour l'affichage (sudo apt install libsdl2-dev)

pb :
scale de l'image, img2 si on affiche juste ces width et height sa depasse l'écran
tenté de scale avec les parametres de l'écran mais marche pas trop donc /4 pour l'instant

a faire:
ajouter un truc pour qu'a l'exec du prog l'utilisateur choisise l'image (recherche dans les fichier probablement)
si motivé, faire une interface de con qui pourra etre réutilisable pour les autre exec

#########################################################################################################################################################

MonPG2:

#########################################################################################################################################################

MonPG3:

#########################################################################################################################################################

MonPG4:

#########################################################################################################################################################

virus :
